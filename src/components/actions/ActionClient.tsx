import { Client } from '@/models/client'

interface Props {
  record: Client
}

const ActionEmployee = ({ record }: Props) => {
  return (
    <div>
      
    </div>
  )
}

export default ActionEmployee
